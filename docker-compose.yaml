version: '3.9'

services:
  database:
    restart: unless-stopped
    image: postgres:14.1
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    ports:
      - '5432:5432'
  redis:
    image: redis:6.2
    ports:
      - '6379:6379'
